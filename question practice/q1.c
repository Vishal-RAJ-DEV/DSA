#include <stdio.h>
//write a program of smallest number of two.

int main(){
    int a,b;//let a,b are two number
    printf("a : ");
    scanf("%d",&a);
    printf("b :");
    scanf("%d",&b);

    if(a<b){
        printf("a is smaller than b");
    }
    else{
        printf("a is greater than b");
    }

    return 0;




}