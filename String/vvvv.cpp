#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main(){
    string s;
    s="vishal";
    cout<<sizeof(s);//calculate the size in bits;
    cout<<s.size();//calculate the size in charactr;
    cout<<s[1];

    return 0;
}